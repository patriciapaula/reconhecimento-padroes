function [C]=metodo1_cov(X)

[N p]=size(X);
soma=zeros(p);
[N p]=size(X);

m=sum(X)/N; % vetor de medias
for i=1:N,
    soma = soma + (X(i,:)-M)’*(X(i,:)-M);
end
C=soma/N; % Matriz de covariancia

    m=sum(X)/N;   % média
    N=size(X,1);
    p=size(X,2);
   
    x=randn(N,1);
        
    %soma(N,N)=0;    
    %for i=1:N,
    %    soma(i)=(1/N)*sum((X(i)-m)*(X(i)-m)');
    %end
end
